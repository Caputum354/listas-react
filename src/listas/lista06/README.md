# Lista 06 — POO: Classes (Nível Fácil)

Tema: **criação de classes, atributos e métodos básicos**.

1. Classe `Livro` com dois objetos instanciados
2. Método `falar()` na classe `Pessoa`
3. `Retangulo` com `calcularArea()`
4. `Carro` com velocidade e `acelerar()`
5. `Produto` com `aplicarDesconto(porcentagem)`
6. `Contador` com incrementar/decrementar
7. `Temperatura` com `paraFahrenheit()`
8. Três instâncias de `Animal`
9. `Filme` — verificar se tem mais de 10 anos
10. `Lampada` com `ligar()` e `desligar()`
