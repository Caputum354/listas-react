# Lista 08 — POO: Lógica e Integração (Nível Difícil)

Tema: **composição, múltiplos objetos e lógica de negócio** (exercícios 21–30).

21. `Carrinho` com array de `Produto`, adicionar e total
22. `Biblioteca` com `buscarPorAutor(nome)`
23. RPG: `Personagem` com `atacar(inimigo)`
24. `Empresa` listando veículos que precisam de manutenção
25. Override de `calcularSalario()` em `Vendedor` e `Diretor`
26. `transferir(valor, contaDestino)` entre contas
27. `Agenda` que exclui `Contato` pelo nome
28. `Estacionamento` com limite de vagas
29. `Playlist` com `ordenarPorDuracao()`
30. Simulador de `Elevador` com limites
