# Lista 13 — Map e Desestruturação (Nível Fácil)

Tema: **uso direto de `.map()` e desestruturação básica**.

1. Dobrar valores com `.map()`
2. Primeira letra maiúscula
3. Desestruturar `['Maçã', 'Banana']` em `f1` e `f2`
4. Troca de variáveis sem temporária
5. Extrair `nome` de um objeto
6. `.map()` com índice — "Índice 0: Valor X"
7. Números → strings
8. Extrair só os nomes das cidades
9. Valor padrão na desestruturação (`cor = "preto"`)
10. Rest operator — `primeiro` e `...outros`
