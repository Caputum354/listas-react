# Lista 07 — POO: Herança e Reuso (Nível Médio)

Tema: **`extends`, `super()` e especialização de classes** (exercícios 11–20).

11. `Cachorro` herda de `Animal` e sobrescreve `emitirSom()`
12. `Funcionario` e subclasse `Gerente` com departamento
13. `super()` repassando nome e salário ao pai
14. `Conta` e `ContaPoupanca` com `aplicarJuros()`
15. `Veiculo`, `Aviao` e `Carro` com `mover()` diferentes
16. Validação de saque (saldo nunca negativo)
17. `Forma`, `Circulo` e `Quadrado` com `desenhar()`
18. `Usuario`, `Professor` e `Aluno` com métodos próprios
19. `ItemPedido` com cálculo do total
20. `CarroEletrico` herda de `Carro` e adiciona bateria
